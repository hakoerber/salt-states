        zone "{{ params.domain }}" IN {
                type slave;
                masters {
                        {{ params.master.ip }};
                };
                file "slaves/forward.{{ params.domain }}";
        };

        zone "{{ params.domain_reverse }}" IN {
                type slave;
                masters {
                        {{ params.master.ip }};
                };
                file "slaves/reverse.{{ params.domain }}";
        };
